<div class="mt-4">
    <div class="row">
        {{#each courses}}
            <div class="col-sm-6 col-lg-4">
                <div class="card card-course-item" >
                    <a href="/courses/{{this._id}}">
                        <img src="{{this.image}}" class="card-img-top card-img-width" alt="..."  >
                    </a>
                    <div class="card-body">
                        <a href="/courses/{{this._id}}">
                            <h5 class="card-title">{{this.name}}</h5>
                        </a>
                        <p class="card-text">{{this.description}}</p>
                        <a href="/courses/{{this._id}}" class="btn btn-primary">Mua Khoá học</a>
                    </div>
                </div>
            </div>
            <script>
                

            </script>
        {{/each}}
    </div> 
    

